# Plan: prj-8554-task-mugtlags

**Project:** `PRJ-8571`  
**Task ID:** `task-mugtlags`  
**Repo:** `prj-8554-task-mugtlags`  

## Summary

Декомпозирую задачу на 4 этапа: setup проекта → CRUD endpoints → middleware/валидация → тесты и документация. Каждый этап выполняется последовательно одним разработчиком (round-robin).

## Subtasks

### 1. Инициализация проекта Express

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать package.json, установить express v2 (последняя мажорная v2.x), настроить структуру каталогов (src/, src/routes/, src/controllers/, src/models/, src/middleware/), создать базовый server.js с подключением express и прослушкой порта из env.
- **Test plan:** npm install проходит без ошибок; npm start запускает сервер на PORT (по умолчанию 3000); GET / возвращает 200 или информационное сообщение; в package.json указана зависимость express ^2.x

### 2. Реализация CRUD endpoints для заметок

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать routes/notes.js с маршрутами GET /notes, GET /notes/:id, POST /notes, PUT /notes/:id, DELETE /notes/:id. Добавить контроллеры notesController.js с логикой обработки. Использовать in-memory хранилище (массив) на данном этапе — БД подключается позже или заменяется через слой models.
- **Test plan:** curl GET /notes возвращает массив; POST /notes с JSON {title, content} создаёт заметку и возвращает 201; GET /notes/:id возвращает заметку; PUT обновляет; DELETE удаляет и возвращает 204. Все ответы в JSON.
- **Dependencies:** `sub-1`

### 3. Middleware и валидация

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить middleware: express.json() (встроенный в express v2), logger (morgan или кастомный), обработчик ошибок с централизованным error handler. Реализовать валидацию входных данных заметки (title обязателен, content обязателен, длина title ≤ 200).
- **Test plan:** POST /notes без title возвращает 400 с понятным сообщением; POST с title длиной > 200 возвращает 400; невалидный JSON возвращает 400; сервер логирует запросы; ошибочные маршруты возвращают JSON 404, а не HTML
- **Dependencies:** `sub-2`

### 4. Тесты и README

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Установить jest + supertest. Написать интеграционные тесты для всех CRUD endpoints (минимум 8 кейсов: create, list, get, update, delete, validation fail, 404, невалидный JSON). Написать README.md с инструкцией запуска и описанием API.
- **Test plan:** npm test проходит все тесты без ошибок; покрытие endpoints >= 80%; README содержит секции Install, Run, Test, API Endpoints со списком маршрутов и примерами curl
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
